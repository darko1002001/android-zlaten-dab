<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="AppTheme.Base">
    <!-- Customize your theme here. -->
    <item name="toolbarStyle">@style/ToolbarStyle</item>
  </style>

  <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Customize your theme here. -->

    <!-- colorPrimary is used for the default action bar background -->
    <item name="colorPrimary">@color/colorPrimary</item>

    <!-- colorPrimaryDark is used for the status bar -->
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

    <!-- colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets -->
    <item name="colorAccent">@color/colorAccent</item>

    <!-- You can also set colorControlNormal, colorControlActivated
         colorControlHighlight & colorSwitchThumbNormal. -->
  </style>

  <style name="ToolbarStyle" parent="@style/Widget.AppCompat.Toolbar">
    <item name="android:paddingLeft">15dp</item>
  </style>

  <style name="CustomBetterPickerTheme">
    <item name="bpDialogBackground">@color/colorPrimary</item>
    <item name="bpTextColor">@color/textColorPrimary</item>
    <item name="bpButtonBackground">@color/colorPrimary</item>
    <item name="bpDividerColor">@color/zlatenDab</item>
  </style>

</resources>
